{{#if resume.languages.length}}
    <div class="container languages-container">
        {{> title value="Languages"}}

        <ul class="minimal">
            {{#each resume.languages}}
                <li>
                    <h4 class="bold">{{language}} {{#if fluency}}<em>({{fluency}})</em>{{/if}}</h4>
                </li>
            {{/each}}
        </ul>
    </div>
{{/if}}